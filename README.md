# lsmlite
This is an implementation of an LSM Tree style storage engine. As an homage to `sqlite`, this implementation is called lsmlite.

This is purely a durable storage engine, a database API and perhaps a read through cache can be added on top to create the entire database engine.

See `docs/protocol.md` for the implementation details. The implementation is complete, with optimizations coming.
